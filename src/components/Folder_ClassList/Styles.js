import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
    inputField: {
       background: 'white',
       color:'black'
    },

    submit_btn: {
        marginTop:'1rem',
    }
    
}));



export default useStyles
